SECRET_KEY=change-me
DEBUG=False
ALLOWED_HOSTS=localhost,127.0.0.1

# DB engine: sqlite or postgres
DB_ENGINE=sqlite

# PostgreSQL settings (used only when DB_ENGINE=postgres)
DB_NAME=events_db
DB_USER=events_user
DB_PASSWORD=change-me
DB_HOST=localhost
DB_PORT=5432
CORS_ALLOWED_ORIGINS=http://localhost:3000
